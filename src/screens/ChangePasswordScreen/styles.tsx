import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({

    userTitle: {
        color: 'black',
        fontSize: 20,
        fontWeight: 'bold',
        marginBottom: '5%',
    },
    inputsContainer: {
        paddingHorizontal: '8%',
        // zIndex: -4,
    },

    container: {
        flex: 1,
        justifyContent: 'center',
    },



});